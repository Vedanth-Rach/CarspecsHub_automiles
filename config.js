// Runtime frontend config.
// Deployers should update this file (or overwrite it during static deploy) to
// set the backend API base url, for example:
// window.API_BASE = 'https://cars-api.onrender.com';

window.API_BASE = window.API_BASE || '';
