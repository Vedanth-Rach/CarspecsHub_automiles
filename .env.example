# Copy this file to .env and fill in real values. Do NOT commit your real .env to version control.

# MongoDB connection string (Atlas). Replace <username>, <password>, and <dbname>.
MONGODB_URI="mongodb+srv://vedhurach_db_user:vedu@0907@cluster0.1enhqme.mongodb.net/?appName=Cluster0"

# Port the server should listen on
PORT=3000

# Node environment
NODE_ENV=development

# Session secret or other app secrets (keep private)
SESSION_SECRET=your_session_secret_here
